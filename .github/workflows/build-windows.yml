name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write   # required for action-gh-release

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --onefile --console --name dracoon-pyclient --collect-all rich dracoon-pyclient.py

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: dracoon-pyclient-windows
          path: dist/dracoon-pyclient.exe

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2.3.2
        with:
          files: dist/dracoon-pyclient.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
